name: my-workflow
on:
    push:
        branches:
            - main
jobs:
    say-hello:
        runs-on: ubuntu-latest
        steps:
        - name: print msg
          run: echo "kaam kr rha hai"
